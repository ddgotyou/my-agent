{
  "name": "my-agent",
  "version": "1.0.0",
  "description": "全栈项目 - React Native + FastAPI",
  "scripts": {
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  },
  "private": true
}

